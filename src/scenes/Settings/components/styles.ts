import { StyleSheet } from 'react-native'

export default StyleSheet.create({
  row: {
    minHeight: 48,
    flexDirection: 'row',
    alignItems: 'center',
    paddingHorizontal: 8,
  },
  label: {
    flex: 1,
    flexWrap: 'wrap',
  }
})
